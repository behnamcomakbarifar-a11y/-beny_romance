class Chapter {
      final String id;
        final String title;
          final String content;
            final bool isLocked;

              Chapter({
                  required this.id,
                      required this.title,
                          required this.content,
                              this.isLocked = false,
                                });
                                }
                                
}